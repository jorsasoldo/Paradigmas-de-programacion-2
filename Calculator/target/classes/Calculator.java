public class Calculator
{
    import java.util.ArrayList

    public class Calculator
    {
        public <T extends Number> Calculator(T [] a)
        {
            public T average()
            {
                for(int i = 0; i < a.length; i++)
                {
                    static T sum;
                    sum += a[i];
                }

                return sum / a.length;
            }
        }
    }
}
